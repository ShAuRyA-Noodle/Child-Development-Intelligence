# Path to the trained XGBoost model file (optional — falls back to rule engine)
MODEL_PATH=models/xgb_risk_model.json

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Server port
PORT=8000
